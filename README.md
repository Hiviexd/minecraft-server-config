# minecraft-server-config

Docker configuration for a Minecraft server.

## Usage

1. Set up `ops.json`, `whitelist.json`, and `rcon_password` from their respective example files.
2. Run `docker-compose up -d`
